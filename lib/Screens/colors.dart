import 'package:flutter/material.dart';

/// Dark blue color used for text and icons.
const Color wColor = Color.fromARGB(255, 18, 44, 52);

/// Light background color for the screens.
const Color bgColor = Color.fromARGB(255, 252, 247, 248);

/// Turquoise color used for borders and accents.
const Color borderColor = Color.fromARGB(255, 61, 176, 180);

/// Red color used for error messages and alerts.
const Color rColor = Color.fromARGB(255, 139, 38, 53);
